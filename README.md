# Experiments using integer relations for FHE packing

Run tests

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pytest
```

For an example printed to stdout,

```bash
python hoist.py
```
